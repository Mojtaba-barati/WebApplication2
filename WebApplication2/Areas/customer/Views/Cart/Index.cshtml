@using webmarket.Models.viewModel
@model shoppingcart
@{
    ViewData["Title"] = "سبد خرید";
    Layout = "~/Areas/customer/Views/Shared/_Layout.cshtml";
}

<form method="post">
	<br />
	<div class="container">
		<div class="card">
			<div class="card-header bg-dark text-light ml-0">
				<div class="row">
					<div class="col-6" >
						<i class="fa fa-shopping-cart"></i> &nbsp;
						سبد خرید
					</div>
					
					<div class="col-6 text-end">
						<a href="" class="btn btn-outline-info btn-sm">ادامه خرید</a>
					</div>
				</div>
			</div>

			<div class="card-body">
				@foreach (var item in Model.listcards)
				{
					<div class="row">
						<div class="d-none d-lg-block col-lg-1 text-center py-2">
							<img src="~/img/product/@item.product.imgurl" class="rounded" width="100%" />
						</div>
						<div class="col-12 col-lg-6 pt-md-3">
							<h5><strong>@item.product.title</strong></h5>
							<p><small>@Html.Raw(item.product.Description)</small></p>
						</div>
						<div class="col-12  col-lg-5 text-center row">
							<div class="col-4 text-md-right pt-4">
								<h6 class="row">
									
										<small class="col-6">@item.product.price.ToString()</small>
										
										<span class=" col-6">@item.count</span>
									
								</h6>
							</div>
							
							<div class="col-6 col-sm-4 col-lg-2 text-right pt-2">
								<a asp-action="remove" asp-route-cartId="@item.Id" class="btn btn-danger">
									<i class="bi bi-trash-fill"></i>
								</a>
							</div>
						</div>
					</div>
					<hr />
				}


			</div>

			<div class="card-footer">
				<div class="card-footer row">
					<div class="col-3">
						<span class="text-success">جمع فاکتور: @Model.CartTotal تومان</span>
					</div>
					<div class="offset-6 col-9">
						<a class="btn btn-primary">ادامه</a>
					</div>
				</div>
			</div>
		</div>
	</div>
</form>



